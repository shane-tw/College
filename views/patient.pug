extends navbar.pug

block vars
	- var title='Virtual Care - Patients'
	- var description='This is a test'

append body
	.container.mt-4
		h2 Patient
		if (them == null)
			p Either this patient doesn't exist, or you don't have permission to view their details.
		else
			.card.text-center.pt-3
				p
					b ID: 
					| #{them.id}
				p 
					b Name: 
					| #{them.name}
				p
					b Email: 
					| #{them.email}